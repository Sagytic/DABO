## 2024-05-21 - Login Form Accessibility
**Learning:** Found critical accessibility issues in login form: missing `<form>` tag, no labels, and inputs using placeholder as label.
**Action:** Always wrap inputs in a `<form>` tag and provide accessible labels (e.g., using `.visually-hidden` class if visual design requires).
