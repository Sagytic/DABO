## 2026-02-14 - Login Form Accessibility
**Learning:** Legacy forms relied on `div` wrappers and JS alerts for validation, missing semantic HTML and accessibility features.
**Action:** Use semantic `<form>` tags, `.visually-hidden` labels for design-constrained inputs, and native HTML5 validation (`required`) for future form implementations.
